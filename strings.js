const nombre3 = 'Deadpool';
const real = 'Wade Winston';

const todo = nombre3 + ' ' + real;
const template = `El nombre completo es ${nombre3} ${real}`; //esta seria la forma correcta.

console.log(todo);
console.log(template);